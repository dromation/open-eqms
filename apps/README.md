# Open-EQMS Apps

This directory contains user-facing executables and reference applications built on top of the Runtime core.

It is deliberately separate from `examples/`: applications here demonstrate complete vertical slices that compose Runtime APIs, while examples would be small language or API samples.
